<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UltimateHoopers.Pages.HoopersPage"
             xmlns:viewmodels="clr-namespace:UltimateHoopers.ViewModels"
             Title="Hoopers"
             BackgroundColor="{StaticResource PageBackgroundColor}">

    <Grid RowDefinitions="Auto,*,Auto"
          Padding="20">

        <!-- Header with Title -->
        <Grid Grid.Row="0" 
              ColumnDefinitions="*,Auto"
              Margin="0,10,0,20">

            <VerticalStackLayout Grid.Column="0">
                <Label Text="Hoopers"
                       FontSize="28"
                       FontAttributes="Bold"
                       TextColor="{StaticResource PrimaryColor}" />

                <Label Text="Search and connect with players"
                       FontSize="16"
                       TextColor="{StaticResource SecondaryTextColor}" />
            </VerticalStackLayout>

            <!-- Back Button -->
            <Frame Grid.Column="1"
                   CornerRadius="25"
                   HeightRequest="50"
                   WidthRequest="50"
                   Padding="0"
                   BorderColor="{StaticResource BorderColor}"
                   BackgroundColor="{StaticResource CardBackgroundColor}"
                   HasShadow="True">

                <Label Text="←"
                       FontSize="24"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       TextColor="{StaticResource PrimaryTextColor}" />

                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnBackClicked" />
                </Frame.GestureRecognizers>
            </Frame>
        </Grid>

        <!-- Content Area (with scrollable main content) -->
        <Grid Grid.Row="1" RowDefinitions="Auto,Auto,*" RowSpacing="15">
            <!-- Search Bar with username search -->
            <Frame Grid.Row="0"
                   Padding="15,5" 
                   CornerRadius="10" 
                   HasShadow="False"
                   BackgroundColor="Gray" 
                   BorderColor="Black"
                   Margin="0,0,0,0">

                <Grid ColumnDefinitions="Auto,*">
                    <Label Text="🔍" 
                           FontSize="20" 
                           VerticalOptions="Center"
                           TextColor="{StaticResource SecondaryTextColor}" />
                    <Entry Grid.Column="1" 
                           Placeholder="Search hoopers by username..." 
                           PlaceholderColor="{StaticResource SecondaryTextColor}"
                           TextColor="{StaticResource PrimaryTextColor}"
                           Margin="5,0,0,0"
                           x:Name="SearchEntry"
                           BackgroundColor="LightGray"
                           TextChanged="OnSearchTextChanged" />
                </Grid>
            </Frame>

            <!-- Filter Options -->
            <ScrollView Grid.Row="1"
                        Orientation="Horizontal" 
                        Margin="0,0,0,0"
                        HorizontalScrollBarVisibility="Never">
                <HorizontalStackLayout Spacing="10">
                    <Frame Padding="15,8" 
                           CornerRadius="20" 
                           BackgroundColor="{StaticResource PrimaryColor}">
                        <Label Text="All Users" 
                               TextColor="White" 
                               FontAttributes="Bold" />
                    </Frame>
                    <Frame Padding="15,8" 
                           CornerRadius="20" 
                           BackgroundColor="{StaticResource CardBackgroundColor}"
                           BorderColor="{StaticResource BorderColor}">
                        <Label Text="@Guards" 
                               TextColor="{StaticResource PrimaryTextColor}" />
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnFilterGuardsClicked" />
                        </Frame.GestureRecognizers>
                    </Frame>
                    <Frame Padding="15,8" 
                           CornerRadius="20" 
                           BackgroundColor="{StaticResource CardBackgroundColor}"
                           BorderColor="{StaticResource BorderColor}">
                        <Label Text="@Forwards" 
                               TextColor="{StaticResource PrimaryTextColor}" />
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnFilterForwardsClicked" />
                        </Frame.GestureRecognizers>
                    </Frame>
                    <Frame Padding="15,8" 
                           CornerRadius="20" 
                           BackgroundColor="{StaticResource CardBackgroundColor}"
                           BorderColor="{StaticResource BorderColor}">
                        <Label Text="@Centers" 
                               TextColor="{StaticResource PrimaryTextColor}" />
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnFilterCentersClicked" />
                        </Frame.GestureRecognizers>
                    </Frame>
                    <Frame Padding="15,8" 
                           CornerRadius="20" 
                           BackgroundColor="{StaticResource CardBackgroundColor}"
                           BorderColor="{StaticResource BorderColor}">
                        <Label Text="@Nearby" 
                               TextColor="{StaticResource PrimaryTextColor}" />
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnFilterNearbyClicked" />
                        </Frame.GestureRecognizers>
                    </Frame>
                </HorizontalStackLayout>
            </ScrollView>

            <!-- Scrollable Players List Section with Loading Indicator -->
            <Grid Grid.Row="2">
                <!-- Loading Indicator-->
                <ActivityIndicator IsRunning="{Binding IsLoading}"
                                  IsVisible="{Binding IsLoading}"
                                  Color="{StaticResource PrimaryColor}"
                                  HorizontalOptions="Center"
                                  VerticalOptions="Center"
                                  HeightRequest="80"
                                  WidthRequest="80"
                                  Scale="2" />

                <!-- Content (visible when not loading) -->
                <ScrollView x:Name="PlayersScrollView" 
                          IsVisible="{Binding IsNotLoading}">
                    <VerticalStackLayout x:Name="PlayersContainer" Spacing="15">
                        <!-- Hoopers List Title -->
                        <Label Text="Featured Hoopers" 
                               FontSize="20"
                               FontAttributes="Bold" 
                               Margin="0,0,0,0" />

                        <!-- Template for Player Cards -->
                        <CollectionView x:Name="HoopersCollection" 
                                       ItemsSource="{Binding FilteredHoopers}"
                                       SelectionMode="None"
                                       EmptyView="No players found. Try another search."
                                       Margin="0,0,0,0">

                            <!-- Add ItemsSpacing property for space between items -->
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                            </CollectionView.ItemsLayout>

                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame 
                                           Padding="0"
                                           BorderColor="{StaticResource BorderColor}"
                                           CornerRadius="10"
                                           HasShadow="True"
                                           BackgroundColor="{StaticResource CardBackgroundColor}"
                                           Margin="0,0,0,0">
                                        <Grid RowSpacing="0" ColumnSpacing="10">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!-- Profile Picture -->
                                            <Frame Grid.Column="0" 
                                                   Grid.RowSpan="2"
                                                   HeightRequest="70" 
                                                   WidthRequest="70"
                                                   CornerRadius="35"
                                                   Padding="0"
                                                   IsClippedToBounds="True"
                                                   BorderColor="{StaticResource PrimaryColor}"
                                                   Margin="15">
                                                <Grid>
                                                    <!-- Profile Image Placeholder (ColorBox with Initials) -->
                                                    <Rectangle x:Name="ProfileColorPlaceholder" 
                                                               Fill="{Binding InitialsColor}"
                                                               VerticalOptions="Fill"
                                                               HorizontalOptions="Fill" />

                                                    <!-- Initials on Placeholder -->
                                                    <Label Text="{Binding Initials}"
                                                           TextColor="White"
                                                           FontSize="24"
                                                           FontAttributes="Bold"
                                                           HorizontalOptions="Center"
                                                           VerticalOptions="Center" />

                                                    <!-- Actual Profile Image (if available) -->
                                                    <Image Source="{Binding ProfileImage}" 
                                                           Aspect="AspectFill"
                                                           IsVisible="{Binding HasValidImage}"
                                                           HorizontalOptions="Fill"
                                                           VerticalOptions="Fill" />
                                                </Grid>
                                            </Frame>

                                            <!-- Player Info Section -->
                                            <VerticalStackLayout Grid.Column="1" 
                                                               Grid.Row="0"
                                                               Spacing="0"
                                                               Margin="0,15,0,0">

                                                <!-- Username and Rank -->
                                                <HorizontalStackLayout>
                                                    <Label Text="{Binding UsernameDisplay}"
                                                           FontSize="18"
                                                           FontAttributes="Bold"
                                                           TextColor="{StaticResource PrimaryColor}" />

                                                    <!-- Rank Badge -->
                                                    <Frame BackgroundColor="{StaticResource PrimaryColor}"
                                                           CornerRadius="10"
                                                           HeightRequest="20"
                                                           Padding="5,0"
                                                           Margin="10,0,0,0">
                                                        <HorizontalStackLayout>
                                                            <Label Text="#" 
                                                                   FontSize="12"
                                                                   TextColor="White"
                                                                   VerticalOptions="Center" />
                                                            <Label Text="{Binding Rank}"
                                                                   FontSize="12"
                                                                   FontAttributes="Bold"
                                                                   TextColor="White"
                                                                   VerticalOptions="Center" />
                                                        </HorizontalStackLayout>
                                                    </Frame>
                                                </HorizontalStackLayout>

                                                <!-- Position and Location - WITHOUT any bottom margin -->
                                                <Label Text="{Binding PositionLocation}"
                                                       FontSize="14"
                                                       Margin="0,0,0,0"
                                                       TextColor="{StaticResource SecondaryTextColor}" />

                                                <!-- Stats Row - WITH negative top margin to move it up -->
                                                <HorizontalStackLayout Spacing="15" 
                                                                      Margin="0,3,0,10">
                                                    <!-- Games Stat -->
                                                    <VerticalStackLayout HorizontalOptions="Start">
                                                        <Label Text="{Binding GamesPlayed}"
                                                               FontAttributes="Bold"
                                                               HorizontalOptions="Center"
                                                               TextColor="{StaticResource PrimaryColor}" />
                                                        <Label Text="Games"
                                                               FontSize="12"
                                                               HorizontalOptions="Center"
                                                               TextColor="{StaticResource SecondaryTextColor}" />
                                                    </VerticalStackLayout>

                                                    <!-- Record Stat -->
                                                    <VerticalStackLayout>
                                                        <Label Text="{Binding Record}"
                                                               FontAttributes="Bold"
                                                               HorizontalOptions="Center"
                                                               TextColor="{StaticResource PrimaryColor}" />
                                                        <Label Text="Record"
                                                               FontSize="12"
                                                               HorizontalOptions="Center"
                                                               TextColor="{StaticResource SecondaryTextColor}" />
                                                    </VerticalStackLayout>

                                                    <!-- Win % Stat -->
                                                    <VerticalStackLayout>
                                                        <Label Text="{Binding WinPercentage}"
                                                               FontAttributes="Bold"
                                                               HorizontalOptions="Center"
                                                               TextColor="{StaticResource PrimaryColor}" />
                                                        <Label Text="Win %"
                                                               FontSize="12"
                                                               HorizontalOptions="Center"
                                                               TextColor="{StaticResource SecondaryTextColor}" />
                                                    </VerticalStackLayout>

                                                    <!-- Rating Stat -->
                                                    <VerticalStackLayout>
                                                        <HorizontalStackLayout HorizontalOptions="Center">
                                                            <Label Text="{Binding RatingDisplay}"
                                                                   FontAttributes="Bold"
                                                                   TextColor="{StaticResource PrimaryColor}" />
                                                            <Label Text="⭐"
                                                                   FontSize="12"
                                                                   TextColor="{StaticResource PrimaryColor}"
                                                                   Margin="2,0,0,0" />
                                                        </HorizontalStackLayout>
                                                        <Label Text="Rating"
                                                               FontSize="12"
                                                               HorizontalOptions="Center"
                                                               TextColor="{StaticResource SecondaryTextColor}" />
                                                    </VerticalStackLayout>
                                                </HorizontalStackLayout>
                                            </VerticalStackLayout>

                                            <!-- Connect Button -->
                                            <Button Grid.Column="2"
                                                    Grid.Row="0"
                                                    Text="Connect"
                                                    BackgroundColor="{StaticResource PrimaryColor}"
                                                    TextColor="White"
                                                    CornerRadius="20"
                                                    HeightRequest="40"
                                                    WidthRequest="100"
                                                    Margin="0,15,15,0"
                                                    CommandParameter="{Binding .}"
                                                    Clicked="OnConnectButtonClicked" />
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </ScrollView>
            </Grid>
        </Grid>

        <!-- Navigation Footer -->
        <Grid Grid.Row="2"
              ColumnDefinitions="*,*,*,*"
              BackgroundColor="{StaticResource CardBackgroundColor}"
              HeightRequest="70"
              Margin="-20,0,-20,0"
              Padding="0,5,0,20">

            <VerticalStackLayout Grid.Column="0"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center"
                                 Spacing="2">
                <Label Text="🏠"
                       FontSize="20"
                       HorizontalOptions="Center"
                       TextColor="{StaticResource SecondaryTextColor}" />
                <Label Text="Home"
                       FontSize="12"
                       TextColor="{StaticResource SecondaryTextColor}"
                       HorizontalOptions="Center" />

                <VerticalStackLayout.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnHomeNavigationClicked" />
                </VerticalStackLayout.GestureRecognizers>
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Column="1"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center"
                                 Spacing="2">
                <Label Text="👥"
                       FontSize="20"
                       HorizontalOptions="Center"
                       TextColor="{StaticResource PrimaryColor}" />
                <Label Text="Hoopers"
                       FontSize="12"
                       TextColor="{StaticResource PrimaryColor}"
                       FontAttributes="Bold"
                       HorizontalOptions="Center" />
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Column="2"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center"
                                 Spacing="2"
                                 Opacity="0.6">
                <Label Text="🏀"
                       FontSize="20"
                       HorizontalOptions="Center" />
                <Label Text="Games"
                       FontSize="12"
                       TextColor="{StaticResource SecondaryTextColor}"
                       HorizontalOptions="Center" />
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Column="3"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center"
                                 Spacing="2"
                                 Opacity="0.6">
                <Label Text="👤"
                       FontSize="20"
                       HorizontalOptions="Center" />
                <Label Text="Profile"
                       FontSize="12"
                       TextColor="{StaticResource SecondaryTextColor}"
                       HorizontalOptions="Center" />
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>